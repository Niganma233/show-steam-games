## 🎮 Add a Games Page to Mizuki

这是一个适用于 [Mizuki Theme](https://github.com/matsuzaka-yuki/Mizuki) 的游戏展示页扩展。

### ✨ 特性
- 适配 Mizuki的风格
- 响应式网格布局
- 支持显示游戏封面、评分、状态（通关/在玩/搁置）

### 🚀 使用方法

***

### 🎮 游戏页面配置指南

#### 1. 添加页面文件
将下载的文件重命名为 `games.astro`，并放入项目的页面目录中。

*   **文件路径**：`Mizuki/src/pages/games.astro`

---

#### 2. 更新类型定义
为了防止 TypeScript 报错，请先添加类型定义。

*   **文件路径**：`Mizuki/src/types/config.ts`
*   **操作**：在 `featurePages` 接口中添加以下代码：

```typescript
featurePages: {
  // ... 其他现有配置
  games: boolean; 
}
```

---

#### 3. 修改配置文件
启用功能并在导航栏中添加入口。

*   **文件路径**：`Mizuki/src/config.ts`

**第一步：启用功能开关**
在 `featurePages` 函数对象中添加：

```typescript
featurePages: {
  // ... 其他现有配置
  games: true, 
},
```

**第二步：配置导航栏**
在 `navBarConfig` 的 `My` 分类（或其他你想要的位置）下添加：

```typescript
export const navBarConfig: NavBarConfig = [
  {
    // ...
    	name: "My",
		url: "/content/",
		icon: "material-symbols:person",
      // ... 其他导航项
               {
					name: "游戏",
					url: "/games/",
					icon: "material-symbols:stadia-controller",
               },
  },
  // ...
];
```

### ⚠️ 注意事项
因为我并没有多语言需求，所以没有添加多语言支持，想要多语言请自行进行修改